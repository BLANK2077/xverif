version: 1
examples:
  - id: key-signals-multi-time-check
    backend: xdebug
    action: value.at
    session: case_a
    args:
      list: ready_path
      times: [100ns, 120ns]
      clock: top.clk
