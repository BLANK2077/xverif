interface:
  display_name: "x-npi"
  short_description: "Python NPI analysis, URG coverage export, and exclusion CSV/EL scripts"
  default_prompt: "Use $x-npi for Python pynpi waveform/protocol/design analysis; use its URG helpers for coverage reads and its NPI helpers only for exclusion target traversal and CSV/EL operations."
